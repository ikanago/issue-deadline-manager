name: Run actions

on:
  pull_request:
    branches:
      - main
      - 'releases/*'

env:
  CARGO_INCREMENTAL: 0

jobs:
  actions:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2

      - uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          override: true
          toolchain: stable

      - name: Cache dependencies
        uses: Swatinem/rust-cache@v1

      - name: Build
        run: cargo build --verbose

      - name: Run actions
        uses: ikanago/issue-deadline-manager

